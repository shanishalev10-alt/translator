"use strict";
// Copyright 2022 DeepL SE (https://www.deepl.com)
// Use of this source code is governed by an MIT
// license that can be found in the LICENSE file.
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseTranslationMemoryJob = exports.parseTranslationMemoryExport = exports.parseTranslationMemoryImport = exports.parseTranslationMemorySegments = exports.parseTranslationMemoryInfoList = exports.parseTranslationMemoryInfoJson = exports.parseTranslationMemoryInfo = exports.parseStyleRuleInfoList = exports.parseStyleRuleInfo = exports.parseCustomInstruction = exports.parseDocumentHandle = exports.parseGlossaryLanguagePairArray = exports.parseLanguageArray = exports.parseWriteResultArray = exports.parseTextResultArray = exports.parseUsage = exports.parseDocumentStatus = exports.parseGlossaryInfoList = exports.parseMultilingualGlossaryInfo = exports.parseGlossaryInfo = exports.parseListMultilingualGlossaries = exports.parseMultilingualGlossaryDictionaryEntries = exports.parseMultilingualGlossaryDictionaryInfo = void 0;
const glossaryEntries_1 = require("./glossaryEntries");
const errors_1 = require("./errors");
const utils_1 = require("./utils");
class UsageDetailImpl {
    /**
     * @private Package users should not need to construct this class.
     */
    constructor(count, limit) {
        this.count = count;
        this.limit = limit;
    }
    limitReached() {
        return this.count >= this.limit;
    }
}
class UsageImpl {
    /**
     * @private Package users should not need to construct this class.
     */
    constructor(character, document, teamDocument) {
        this.character = character;
        this.document = document;
        this.teamDocument = teamDocument;
    }
    /** Returns true if any usage type limit has been reached or passed, otherwise false. */
    anyLimitReached() {
        var _a, _b, _c;
        return (((_a = this.character) === null || _a === void 0 ? void 0 : _a.limitReached()) ||
            ((_b = this.document) === null || _b === void 0 ? void 0 : _b.limitReached()) ||
            ((_c = this.teamDocument) === null || _c === void 0 ? void 0 : _c.limitReached()) ||
            false);
    }
    /** Converts the usage details to a human-readable string. */
    toString() {
        const labelledDetails = [
            ['Characters', this.character],
            ['Documents', this.document],
            ['Team documents', this.teamDocument],
        ];
        const detailsString = labelledDetails
            .filter(([, detail]) => detail)
            .map(([label, detail]) => `${label}: ${detail.count} of ${detail.limit}`);
        return 'Usage this billing period:\n' + detailsString.join('\n');
    }
}
class DocumentStatusImpl {
    constructor(status, secondsRemaining, billedCharacters, errorMessage) {
        this.status = status;
        this.secondsRemaining = secondsRemaining;
        this.billedCharacters = billedCharacters;
        this.errorMessage = errorMessage;
    }
    ok() {
        return this.status === 'queued' || this.status === 'translating' || this.status === 'done';
    }
    done() {
        return this.status === 'done';
    }
}
/**
 * Parses the given glossary info API response to a GlossaryInfo object.
 * @private
 */
function parseRawGlossaryInfo(obj) {
    return {
        glossaryId: obj.glossary_id,
        name: obj.name,
        ready: obj.ready,
        sourceLang: obj.source_lang,
        targetLang: obj.target_lang,
        creationTime: new Date(obj.creation_time),
        entryCount: obj.entry_count,
    };
}
/**
 * Parses the given multilingual glossary info API response to a GlossaryInfo object.
 * @private
 */
function parseMultilingualGlossaryDictionaryInfo(obj) {
    return {
        sourceLangCode: obj.source_lang,
        targetLangCode: obj.target_lang,
        entryCount: obj.entry_count,
    };
}
exports.parseMultilingualGlossaryDictionaryInfo = parseMultilingualGlossaryDictionaryInfo;
/**
 * Parses the given multilingual glossary info API response to a GlossaryInfo object.
 * @private
 */
function parseRawMultilingualGlossaryInfo(obj) {
    return {
        glossaryId: obj.glossary_id,
        name: obj.name,
        creationTime: new Date(obj.creation_time),
        dictionaries: obj.dictionaries.map((dict) => parseMultilingualGlossaryDictionaryInfo(dict)),
    };
}
/**
 * Parses the given multilingual glossary entries API response to a GlossaryDictionaryEntries object.
 * @private
 */
function parseMultilingualGlossaryDictionaryEntries(obj) {
    return obj.dictionaries.map((dict) => ({
        sourceLangCode: dict.source_lang,
        targetLangCode: dict.target_lang,
        entries: new glossaryEntries_1.GlossaryEntries({ tsv: dict.entries }),
    }));
}
exports.parseMultilingualGlossaryDictionaryEntries = parseMultilingualGlossaryDictionaryEntries;
/**
 * Parses the given list multilingual glossaries API response.
 * @private
 */
function parseListMultilingualGlossaries(obj) {
    return obj.glossaries.map((glossary) => ({
        glossaryId: glossary.glossary_id,
        name: glossary.name,
        dictionaries: glossary.dictionaries.map((dict) => parseMultilingualGlossaryDictionaryInfo(dict)),
        creationTime: new Date(glossary.creation_time),
    }));
}
exports.parseListMultilingualGlossaries = parseListMultilingualGlossaries;
/**
 * Parses the given JSON string to a GlossaryInfo object.
 * @private
 */
function parseGlossaryInfo(json) {
    try {
        const obj = JSON.parse(json);
        return parseRawGlossaryInfo(obj);
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseGlossaryInfo = parseGlossaryInfo;
/**
 * Parses the given JSON string to a MultilingualGlossaryInfo object.
 * @private
 */
function parseMultilingualGlossaryInfo(json) {
    try {
        const obj = JSON.parse(json);
        return parseRawMultilingualGlossaryInfo(obj);
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseMultilingualGlossaryInfo = parseMultilingualGlossaryInfo;
/**
 * Parses the given JSON string to an array of GlossaryInfo objects.
 * @private
 */
function parseGlossaryInfoList(json) {
    try {
        const obj = JSON.parse(json);
        return obj.glossaries.map((rawGlossaryInfo) => parseRawGlossaryInfo(rawGlossaryInfo));
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseGlossaryInfoList = parseGlossaryInfoList;
/**
 * Parses the given JSON string to a DocumentStatus object.
 * @private
 */
function parseDocumentStatus(json) {
    try {
        const obj = JSON.parse(json);
        return new DocumentStatusImpl(obj.status, obj.seconds_remaining, obj.billed_characters, obj.error_message);
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseDocumentStatus = parseDocumentStatus;
/**
 * Parses the given usage API response to a UsageDetail object, which forms part of a Usage object.
 * @private
 */
function parseUsageDetail(obj, prefix) {
    const count = obj[`${prefix}_count`];
    const limit = obj[`${prefix}_limit`];
    if (count === undefined || limit === undefined)
        return undefined;
    return new UsageDetailImpl(count, limit);
}
/**
 * Parses the given JSON string to a Usage object.
 * @private
 */
function parseUsage(json) {
    try {
        const obj = JSON.parse(json);
        return new UsageImpl(parseUsageDetail(obj, 'character'), parseUsageDetail(obj, 'document'), parseUsageDetail(obj, 'team_document'));
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseUsage = parseUsage;
/**
 * Parses the given JSON string to an array of TextResult objects.
 * @private
 */
function parseTextResultArray(json) {
    try {
        const obj = JSON.parse(json);
        return obj.translations.map((translation) => {
            return {
                text: translation.text,
                detectedSourceLang: (0, utils_1.standardizeLanguageCode)(translation.detected_source_language),
                billedCharacters: translation.billed_characters,
                modelTypeUsed: translation.model_type_used,
            };
        });
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTextResultArray = parseTextResultArray;
function parseWriteResultArray(json) {
    try {
        const obj = JSON.parse(json);
        return obj.improvements.map((improvement) => {
            return {
                text: improvement.text,
                detectedSourceLang: (0, utils_1.standardizeLanguageCode)(improvement.detected_source_language),
                targetLang: improvement.target_language,
            };
        });
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseWriteResultArray = parseWriteResultArray;
/**
 * Parses the given language API response to a Language object.
 * @private
 */
function parseLanguage(lang) {
    try {
        const result = {
            name: lang.name,
            code: (0, utils_1.standardizeLanguageCode)(lang.language),
            supportsFormality: lang.supports_formality,
        };
        if (result.supportsFormality === undefined) {
            delete result.supportsFormality;
        }
        return result;
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
/**
 * Parses the given JSON string to an array of Language objects.
 * @private
 */
function parseLanguageArray(json) {
    const obj = JSON.parse(json);
    return obj.map((lang) => parseLanguage(lang));
}
exports.parseLanguageArray = parseLanguageArray;
/**
 * Parses the given glossary language pair API response to a GlossaryLanguagePair object.
 * @private
 */
function parseGlossaryLanguagePair(obj) {
    try {
        return {
            sourceLang: obj.source_lang,
            targetLang: obj.target_lang,
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
/**
 * Parses the given JSON string to an array of GlossaryLanguagePair objects.
 * @private
 */
function parseGlossaryLanguagePairArray(json) {
    const obj = JSON.parse(json);
    return obj.supported_languages.map((langPair) => parseGlossaryLanguagePair(langPair));
}
exports.parseGlossaryLanguagePairArray = parseGlossaryLanguagePairArray;
/**
 * Parses the given JSON string to a DocumentHandle object.
 * @private
 */
function parseDocumentHandle(json) {
    try {
        const obj = JSON.parse(json);
        return { documentId: obj.document_id, documentKey: obj.document_key };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseDocumentHandle = parseDocumentHandle;
/**
 * Parses the given style rule API response to a ConfiguredRules object.
 * @private
 */
function parseConfiguredRules(configuredRulesData) {
    if (!configuredRulesData) {
        return undefined;
    }
    return {
        datesAndTimes: configuredRulesData.dates_and_times,
        formatting: configuredRulesData.formatting,
        numbers: configuredRulesData.numbers,
        punctuation: configuredRulesData.punctuation,
        spellingAndGrammar: configuredRulesData.spelling_and_grammar,
        styleAndTone: configuredRulesData.style_and_tone,
        vocabulary: configuredRulesData.vocabulary,
    };
}
/**
 * Parses the given custom instruction API response to a CustomInstruction object.
 * @private
 */
function parseCustomInstruction(instruction) {
    return {
        id: instruction.id,
        label: instruction.label,
        prompt: instruction.prompt,
        sourceLanguage: instruction.source_language,
    };
}
exports.parseCustomInstruction = parseCustomInstruction;
/**
 * Parses the given style rule API response to a StyleRuleInfo object.
 * @private
 */
function parseStyleRuleInfo(styleRule) {
    try {
        const customInstructions = styleRule.custom_instructions
            ? styleRule.custom_instructions.map(parseCustomInstruction)
            : undefined;
        return {
            styleId: styleRule.style_id,
            name: styleRule.name,
            creationTime: new Date(styleRule.creation_time),
            updatedTime: new Date(styleRule.updated_time),
            language: styleRule.language,
            version: styleRule.version,
            configuredRules: parseConfiguredRules(styleRule.configured_rules),
            customInstructions,
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseStyleRuleInfo = parseStyleRuleInfo;
/**
 * Parses the given JSON string to an array of StyleRuleInfo objects.
 * @private
 */
function parseStyleRuleInfoList(json) {
    try {
        const obj = JSON.parse(json);
        return obj.style_rules.map((styleRule) => parseStyleRuleInfo(styleRule));
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseStyleRuleInfoList = parseStyleRuleInfoList;
/**
 * Parses an optional API timestamp, returning undefined if it is absent or unparseable.
 * @private
 */
function parseOptionalTimestamp(value) {
    if (!value) {
        return undefined;
    }
    const date = new Date(value);
    return isNaN(date.getTime()) ? undefined : date;
}
/**
 * Parses the given translation memory API response to a TranslationMemoryInfo object.
 * @private
 */
function parseTranslationMemoryInfo(tm) {
    return {
        translationMemoryId: tm.translation_memory_id,
        name: tm.name,
        sourceLanguage: tm.source_language,
        targetLanguages: tm.target_languages,
        segmentCount: tm.segment_count,
        creationTime: parseOptionalTimestamp(tm.creation_time),
        updatedTime: parseOptionalTimestamp(tm.updated_time),
    };
}
exports.parseTranslationMemoryInfo = parseTranslationMemoryInfo;
/**
 * Parses the given JSON string to a TranslationMemoryInfo object.
 * @private
 */
function parseTranslationMemoryInfoJson(json) {
    try {
        return parseTranslationMemoryInfo(JSON.parse(json));
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemoryInfoJson = parseTranslationMemoryInfoJson;
/**
 * Parses the given JSON string to an array of TranslationMemoryInfo objects.
 * @private
 */
function parseTranslationMemoryInfoList(json) {
    try {
        const obj = JSON.parse(json);
        return obj.translation_memories.map(parseTranslationMemoryInfo);
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemoryInfoList = parseTranslationMemoryInfoList;
/**
 * Parses the given JSON string to a TranslationMemorySegments object.
 * @private
 */
function parseTranslationMemorySegments(json) {
    var _a, _b;
    try {
        const obj = JSON.parse(json);
        return {
            segments: ((_a = obj.segments) !== null && _a !== void 0 ? _a : []).map((segment) => {
                var _a;
                return ({
                    sourceSegmentId: segment.source_segment_id,
                    sourceText: segment.source_text,
                    creationTime: parseOptionalTimestamp(segment.creation_time),
                    updatedTime: parseOptionalTimestamp(segment.updated_time),
                    lastUsedTime: parseOptionalTimestamp(segment.last_used_time),
                    targets: ((_a = segment.targets) !== null && _a !== void 0 ? _a : []).map((target) => ({
                        targetSegmentId: target.target_segment_id,
                        targetLanguage: target.target_language,
                        targetText: target.target_text,
                        creationTime: parseOptionalTimestamp(target.creation_time),
                        updatedTime: parseOptionalTimestamp(target.updated_time),
                        lastUsedTime: parseOptionalTimestamp(target.last_used_time),
                    })),
                });
            }),
            segmentCount: (_b = obj.segment_count) !== null && _b !== void 0 ? _b : 0,
            nextPageCursor: obj.next_page_cursor,
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemorySegments = parseTranslationMemorySegments;
/**
 * Parses the given JSON string to a TranslationMemoryImport object.
 * @private
 */
function parseTranslationMemoryImport(json) {
    try {
        const obj = JSON.parse(json);
        return {
            jobId: obj.job_id,
            uploadUrl: obj.upload_url,
            expiresAt: parseOptionalTimestamp(obj.expires_at),
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemoryImport = parseTranslationMemoryImport;
/**
 * Parses the given JSON string to a TranslationMemoryExport object. reusedExisting reflects
 * whether the API answered 200 (reused a completed export) rather than 202 (started a new one).
 * @private
 */
function parseTranslationMemoryExport(json, reusedExisting) {
    var _a;
    try {
        const obj = JSON.parse(json);
        return {
            jobId: obj.job_id,
            translationMemoryId: (_a = obj.parameters) === null || _a === void 0 ? void 0 : _a.translation_memory_id,
            reusedExisting,
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemoryExport = parseTranslationMemoryExport;
/**
 * Parses the given JSON string to a TranslationMemoryJob object.
 * @private
 */
function parseTranslationMemoryJob(json) {
    var _a, _b, _c, _d, _e, _f, _g;
    try {
        const obj = JSON.parse(json);
        const results = ((_a = obj.results) !== null && _a !== void 0 ? _a : []).map((result) => {
            var _a, _b, _c;
            return ({
                status: ((_a = result.status) !== null && _a !== void 0 ? _a : ''),
                requiredAction: (_b = result.status_metadata) === null || _b === void 0 ? void 0 : _b.required_action,
                downloadUrl: result.download_url,
                expiresAt: parseOptionalTimestamp(result.expires_at),
                errorMessage: (_c = result.error) === null || _c === void 0 ? void 0 : _c.message,
                translationMemoryId: result.translation_memory_id,
                skippedSegmentCount: result.skipped_segment_count,
            });
        });
        return {
            jobId: obj.job_id,
            operation: ((_b = obj.operation) !== null && _b !== void 0 ? _b : ''),
            product: (_c = obj.product) !== null && _c !== void 0 ? _c : 'translation_memory',
            results,
            creationTime: parseOptionalTimestamp(obj.creation_time),
            updatedTime: parseOptionalTimestamp(obj.updated_time),
            translationMemoryId: (_d = obj.parameters) === null || _d === void 0 ? void 0 : _d.translation_memory_id,
            displayName: (_e = obj.parameters) === null || _e === void 0 ? void 0 : _e.display_name,
            sourceContentType: (_f = obj.source_file) === null || _f === void 0 ? void 0 : _f.content_type,
            sourceContentLength: (_g = obj.source_file) === null || _g === void 0 ? void 0 : _g.content_length,
        };
    }
    catch (error) {
        throw new errors_1.DeepLError(`Error parsing response JSON: ${error}`);
    }
}
exports.parseTranslationMemoryJob = parseTranslationMemoryJob;
